#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
script="$repo_root/scripts/sync-codex-skills.sh"

if [[ ! -x "$script" ]]; then
  exit 0
fi

if ! "$script"; then
  echo "warn: codex skill sync failed" >&2
fi
